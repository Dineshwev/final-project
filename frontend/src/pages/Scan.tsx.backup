import React from "react";
import GlobalScanContainer from "../scan-modes/global/GlobalScanContainer";

const ScanPage: React.FC = () => {
  return (
    <div className="container mx-auto px-4 py-8">
      <div className="max-w-6xl mx-auto">
        <div className="mb-8 text-center">
          <h1 className="text-3xl font-bold text-gray-900 mb-4">
            Global SEO Analysis
          </h1>
          <p className="text-lg text-gray-600 max-w-2xl mx-auto">
            Run a comprehensive SEO audit to analyze your website's performance, 
            accessibility, technical health, and optimization opportunities.
          </p>
        </div>
        
        {/* Isolated GlobalScanContainer - all functionality contained within */}
        <GlobalScanContainer 
          className="w-full"
          showHistory={true}
        />
      </div>
    </div>
  );
};

export default ScanPage;
      localStorage.setItem('lastScanId', scanId);
      
      setError(null);
      let attempts = 0;
      const maxAttempts = 45;
      while (attempts < maxAttempts) {
        attempts++;
        const stRes = await fetch(`${API_BASE}/scan/${scanId}`);
        const st = await stRes.json();
        const status = st?.data?.status;
        const srvProg = st?.data?.progress ?? 0;
        setProgress(Math.max(10, Math.min(95, srvProg)));
        if (status === "completed") {
          const rRes = await fetch(`${API_BASE}/scan/${scanId}/results`);
          const rData = await rRes.json();
          if (rData.status === "success") {
            setResults(rData.data);
            setProgress(100);
            break;
          }
        }
        if (status === "failed" || status === "cancelled")
          throw new Error(`Scan ${status}`);
        await new Promise((r) => setTimeout(r, 1000));
      }
      if (!results && attempts >= maxAttempts)
        throw new Error("Scan timed out. Please try again.");
    } catch (err: any) {
      console.error(err);
      setError(err?.message || "Scan failed");
    } finally {
      setIsScanning(false);
    }
  };

  const startScan = async (e: React.FormEvent) => {
    e.preventDefault();
    navigate(`/scan?url=${encodeURIComponent(url)}`);
  };

  const buildIssues = (): IssueItem[] => {
    const src = results?.recommendations || [];
    return src.map((r: any) => ({
      category: r.category || "General",
      issue: r.issue || r.text || "Recommendation",
      priority: (r.priority || "low") as "high" | "medium" | "low",
    }));
  };

  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-100 py-12 px-4">
      <div className="max-w-7xl mx-auto">
        {/* Header Section */}
        <div className="mb-10 text-center">
          <div className="inline-flex items-center justify-center w-16 h-16 rounded-2xl bg-gradient-to-br from-blue-600 to-indigo-600 mb-4 shadow-lg">
            <svg
              className="w-8 h-8 text-white"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                strokeLinecap="round"
                strokeLinejoin="round"
                strokeWidth={2}
                d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"
              />
            </svg>
          </div>
          <h1 className="text-5xl font-extrabold bg-gradient-to-r from-gray-900 via-blue-800 to-indigo-900 bg-clip-text text-transparent mb-3">
            Advanced SEO Analysis
          </h1>
          <p className="text-lg text-gray-600 max-w-2xl mx-auto">
            Comprehensive website analysis covering SEO, performance,
            accessibility, security, and best practices.
          </p>
        </div>

        {/* Scan Form */}
        <form
          onSubmit={startScan}
          className="bg-white/80 backdrop-blur-sm rounded-3xl shadow-2xl p-8 max-w-4xl mx-auto border border-white/20 mb-12"
        >
          <div className="flex flex-col md:flex-row gap-4">
            <div className="flex-1 relative">
              <div className="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                <svg
                  className="h-5 w-5 text-gray-400"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    strokeLinecap="round"
                    strokeLinejoin="round"
                    strokeWidth={2}
                    d="M21 12a9 9 0 01-9 9m9-9a9 9 0 00-9-9m9 9H3m9 9a9 9 0 01-9-9m9 9c1.657 0 3-4.03 3-9s-1.343-9-3-9m0 18c-1.657 0-3-4.03-3-9s1.343-9 3-9m-9 9a9 9 0 019-9"
                  />
                </svg>
              </div>
              <input
                type="url"
                value={url}
                onChange={(e) => setUrl(e.target.value)}
                required
                placeholder="https://example.com"
                className="w-full pl-12 pr-4 py-4 rounded-xl border-2 border-gray-200 focus:border-blue-500 focus:ring-4 focus:ring-blue-100 transition-all duration-200 text-gray-900 placeholder-gray-400 font-medium"
              />
            </div>
            <button
              disabled={isScanning}
              className={`px-8 py-4 rounded-xl text-white font-bold text-lg shadow-lg transition-all duration-200 transform hover:scale-105 disabled:transform-none disabled:cursor-not-allowed flex items-center gap-3 ${
                isScanning
                  ? "bg-gradient-to-r from-blue-400 to-indigo-400 cursor-wait"
                  : "bg-gradient-to-r from-blue-600 to-indigo-600 hover:from-blue-700 hover:to-indigo-700 hover:shadow-xl"
              }`}
            >
              {isScanning ? (
                <>
                  <svg
                    className="animate-spin h-5 w-5"
                    fill="none"
                    viewBox="0 0 24 24"
                  >
                    <circle
                      className="opacity-25"
                      cx="12"
                      cy="12"
                      r="10"
                      stroke="currentColor"
                      strokeWidth="4"
                    ></circle>
                    <path
                      className="opacity-75"
                      fill="currentColor"
                      d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"
                    ></path>
                  </svg>
                  Analyzing...
                </>
              ) : (
                <>
                  <svg
                    className="w-5 h-5"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                  >
                    <path
                      strokeLinecap="round"
                      strokeLinejoin="round"
                      strokeWidth={2}
                      d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"
                    />
                  </svg>
                  Start Analysis
                </>
              )}
            </button>
          </div>

          {isScanning && (
            <div className="mt-6 animate-fadeIn">
              <div className="flex justify-between items-center text-sm font-semibold text-gray-700 mb-2">
                <span className="flex items-center gap-2">
                  <span className="relative flex h-3 w-3">
                    <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-blue-400 opacity-75"></span>
                    <span className="relative inline-flex rounded-full h-3 w-3 bg-blue-500"></span>
                  </span>
                  Analyzing website...
                </span>
                <span className="text-blue-600">{progress}%</span>
              </div>
              <div className="w-full bg-gray-200 rounded-full h-3 overflow-hidden shadow-inner">
                <div
                  className="bg-gradient-to-r from-blue-500 via-indigo-500 to-purple-600 h-3 rounded-full transition-all duration-500 ease-out shadow-lg"
                  style={{ width: `${progress}%` }}
                >
                  <div className="h-full w-full bg-white opacity-25 animate-shimmer"></div>
                </div>
              </div>
              <p className="text-xs text-gray-500 mt-2 text-center">
                This may take 30-60 seconds depending on the website size
              </p>
            </div>
          )}

          {error && (
            <div className="mt-4 p-4 bg-red-50 border-l-4 border-red-500 rounded-lg animate-fadeIn">
              <div className="flex items-start gap-3">
                <svg
                  className="w-5 h-5 text-red-500 flex-shrink-0 mt-0.5"
                  fill="currentColor"
                  viewBox="0 0 20 20"
                >
                  <path
                    fillRule="evenodd"
                    d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z"
                    clipRule="evenodd"
                  />
                </svg>
                <div>
                  <h3 className="text-sm font-bold text-red-800">
                    Analysis Failed
                  </h3>
                  <p className="text-sm text-red-700 mt-1">{error}</p>
                </div>
              </div>
            </div>
          )}
        </form>

        {results && (
          <div className="mt-12 space-y-8 animate-fadeIn">
            {/* Enhanced Tab Navigation */}
            <div className="bg-white/80 backdrop-blur-sm rounded-2xl shadow-xl p-2 border border-gray-100">
              <div className="flex flex-wrap gap-2">
                {[
                  { id: "overview", label: "Overview", icon: "📊" },
                  { id: "scores", label: "Scores", icon: "🎯" },
                  { id: "performance", label: "Performance", icon: "⚡" },
                  {
                    id: "device-comparison",
                    label: "Device Comparison",
                    icon: "📱",
                  },
                  { id: "security", label: "Security", icon: "🔒" },
                  {
                    id: "security-headers",
                    label: "Security Headers",
                    icon: "🛡️",
                  },
                  { id: "seo", label: "SEO", icon: "🔍" },
                  { id: "backlinks", label: "Backlinks", icon: "🔗" },
                  {
                    id: "recommendations",
                    label: "Recommendations",
                    icon: "💡",
                  },
                  { id: "content", label: "Content", icon: "📝" },
                  { id: "raw", label: "Raw Data", icon: "📄" },
                ].map((t) => (
                  <button
                    key={t.id}
                    onClick={() => setActiveTab(t.id)}
                    className={`px-4 py-3 rounded-xl text-sm font-semibold transition-all duration-200 flex items-center gap-2 ${
                      activeTab === t.id
                        ? "bg-gradient-to-r from-blue-600 to-indigo-600 text-white shadow-lg scale-105 transform"
                        : "bg-gray-50 text-gray-700 hover:bg-gray-100 hover:shadow-md hover:scale-102"
                    }`}
                    aria-selected={activeTab === t.id}
                    role="tab"
                  >
                    <span className="text-base">{t.icon}</span>
                    <span>{t.label}</span>
                  </button>
                ))}
              </div>
            </div>

            {activeTab === "overview" && (
              <div className="space-y-8" role="tabpanel" aria-label="Overview">
                {/* Summary Header */}
                <div className="bg-gradient-to-br from-white to-blue-50/30 rounded-2xl shadow-xl p-8 border border-gray-100">
                  <div className="flex flex-col md:flex-row md:items-center md:justify-between gap-4">
                    <div className="flex-1">
                      <div className="flex items-center gap-2 text-sm font-semibold text-gray-500 uppercase tracking-wide mb-2">
                        <svg
                          className="w-4 h-4"
                          fill="currentColor"
                          viewBox="0 0 20 20"
                        >
                          <path
                            fillRule="evenodd"
                            d="M12.586 4.586a2 2 0 112.828 2.828l-3 3a2 2 0 01-2.828 0 1 1 0 00-1.414 1.414 4 4 0 005.656 0l3-3a4 4 0 00-5.656-5.656l-1.5 1.5a1 1 0 101.414 1.414l1.5-1.5zm-5 5a2 2 0 012.828 0 1 1 0 101.414-1.414 4 4 0 00-5.656 0l-3 3a4 4 0 105.656 5.656l1.5-1.5a1 1 0 10-1.414-1.414l-1.5 1.5a2 2 0 11-2.828-2.828l3-3z"
                            clipRule="evenodd"
                          />
                        </svg>
                        Analyzed URL
                      </div>
                      <div className="text-xl font-bold text-gray-900 break-all mb-2">
                        {results.url || url}
                      </div>
                      {results.completedAt && (
                        <div className="flex items-center gap-2 text-sm text-gray-600">
                          <svg
                            className="w-4 h-4"
                            fill="none"
                            stroke="currentColor"
                            viewBox="0 0 24 24"
                          >
                            <path
                              strokeLinecap="round"
                              strokeLinejoin="round"
                              strokeWidth={2}
                              d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"
                            />
                          </svg>
                          Completed:{" "}
                          {new Date(results.completedAt).toLocaleString()}
                        </div>
                      )}
                    </div>
                    <div className="flex items-start gap-2 bg-blue-50 rounded-xl p-4 border border-blue-100">
                      <svg
                        className="w-5 h-5 text-blue-600 flex-shrink-0 mt-0.5"
                        fill="currentColor"
                        viewBox="0 0 20 20"
                      >
                        <path
                          fillRule="evenodd"
                          d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z"
                          clipRule="evenodd"
                        />
                      </svg>
                      <p className="text-sm text-gray-700 max-w-xs">
                        Use the tabs above for detailed analysis. This overview
                        provides a comprehensive snapshot.
                      </p>
                    </div>
                  </div>
                </div>

                {/* Scores Snapshot */}
                {results.lighthouse?.scores && (
                  <div>
                    <h2 className="text-2xl font-bold text-gray-900 mb-4 flex items-center gap-2">
                      <span className="text-3xl">🎯</span>
                      Performance Scores
                    </h2>
                    <div className="grid grid-cols-2 md:grid-cols-4 gap-6">
                      <ScoreGauge
                        label="Performance"
                        score={results.lighthouse.scores.performance}
                        color="blue"
                      />
                      <ScoreGauge
                        label="SEO"
                        score={results.lighthouse.scores.seo}
                        color="purple"
                      />
                      <ScoreGauge
                        label="Accessibility"
                        score={results.lighthouse.scores.accessibility}
                        color="green"
                      />
                      <ScoreGauge
                        label="Best Practices"
                        score={results.lighthouse.scores.bestPractices}
                        color="indigo"
                      />
                    </div>
                  </div>
                )}

                {/* Performance + Security */}
                <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
                  <PerformancePanel metrics={results.lighthouse?.metrics} />
                  <SecurityPanel data={results.security} />
                </div>

                {/* SEO Quick Meta */}
                {results.seo?.metadata && (
                  <Section
                    title="Metadata Summary"
                    description="Key meta fields"
                  >
                    <div className="space-y-2 text-sm">
                      {Object.entries(results.seo.metadata)
                        .filter(([k]) => !["issues"].includes(k))
                        .slice(0, 8)
                        .map(([k, v]) => (
                          <div key={k} className="flex justify-between">
                            <span className="text-gray-500 capitalize">
                              {k.replace(/([A-Z])/g, " $1")}
                            </span>
                            <span
                              className="font-medium text-gray-800 max-w-[55%] text-right truncate"
                              title={String(v)}
                            >
                              {String(v)}
                            </span>
                          </div>
                        ))}
                      {Array.isArray(results.seo.metadata.issues) &&
                        results.seo.metadata.issues.length > 0 && (
                          <div className="pt-2 border-t mt-2 text-xs text-red-600">
                            {results.seo.metadata.issues.length} metadata issues
                            detected
                          </div>
                        )}
                    </div>
                  </Section>
                )}

                {/* Backlinks Snapshot */}
                <Section
                  title="Backlinks Snapshot"
                  description="High-level backlink counts"
                >
                  {!backlinks && (
                    <div className="text-sm text-gray-500">
                      Backlinks loading (open Backlinks tab for full view)...
                    </div>
                  )}
                  {backlinks && (
                    <div className="grid grid-cols-2 md:grid-cols-3 gap-4 text-sm">
                      {Object.entries(backlinks)
                        .slice(0, 6)
                        .map(([k, v]) => (
                          <div
                            key={k}
                            className="bg-gray-50 rounded p-3 flex flex-col"
                          >
                            <span className="text-gray-600 capitalize mb-1">
                              {k.replace(/_/g, " ")}
                            </span>
                            <span className="font-semibold text-lg">
                              {String(v)}
                            </span>
                          </div>
                        ))}
                    </div>
                  )}
                </Section>

                {/* Recommendations */}
                <IssueTable
                  title="Top Recommendations"
                  issues={buildIssues().slice(0, 10)}
                />

                {/* Raw Data (collapsible) */}
                <Section
                  title="Raw Data"
                  description="Full JSON payload (collapsed)"
                >
                  <RawDataPanel data={results} />
                </Section>
              </div>
            )}
            {activeTab === "content" && (
              <div role="tabpanel" className="animate-fadeIn">
                <h2 className="text-2xl font-bold text-gray-900 mb-6 flex items-center gap-2">
                  <span className="text-3xl">📝</span>
                  Content Analysis
                </h2>
                <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
                  {!results.content && (
                    <div className="bg-gradient-to-br from-white to-blue-50 rounded-2xl shadow-lg p-8 border border-blue-100 col-span-full">
                      <div className="flex flex-col items-center text-center gap-4">
                        <div className="w-16 h-16 rounded-full bg-blue-100 flex items-center justify-center">
                          <svg
                            className="w-8 h-8 text-blue-600"
                            fill="none"
                            stroke="currentColor"
                            viewBox="0 0 24 24"
                          >
                            <path
                              strokeLinecap="round"
                              strokeLinejoin="round"
                              strokeWidth={2}
                              d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"
                            />
                          </svg>
                        </div>
                        <div>
                          <h3 className="text-xl font-bold text-gray-900 mb-2">
                            Content Analysis Available
                          </h3>
                          <p className="text-gray-600 mb-4">
                            Analyze readability, structure, keyword density, and
                            get optimization suggestions.
                          </p>
                        </div>
                        <button
                          onClick={async () => {
                            try {
                              const res = await fetch(
                                `${API_BASE}/content-analysis?url=${encodeURIComponent(
                                  results.url || url
                                )}`
                              );
                              const data = await res.json();
                              if (data.status === "success") {
                                setResults((prev: any) => ({
                                  ...prev,
                                  content: data.data,
                                }));
                              }
                            } catch (e) {
                              console.error("Content analysis failed", e);
                            }
                          }}
                          className="px-6 py-3 bg-gradient-to-r from-blue-600 to-indigo-600 text-white rounded-xl font-semibold hover:from-blue-700 hover:to-indigo-700 shadow-lg hover:shadow-xl transition-all duration-200 transform hover:scale-105"
                        >
                          Run Content Analysis
                        </button>
                      </div>
                    </div>
                  )}
                  {results.content && (
                    <>
                      <Section
                        title="📖 Readability Scores"
                        description="Comprehensive readability analysis"
                      >
                        {/* Main Scores */}
                        <div className="mb-4">
                          <div className="flex justify-between items-center mb-2">
                            <span className="text-sm font-medium text-gray-700">
                              Flesch Reading Ease
                            </span>
                            <span
                              className="text-2xl font-bold"
                              style={{
                                color:
                                  results.content.readability
                                    .fleschReadingEase >= 60
                                    ? "#22c55e"
                                    : results.content.readability
                                        .fleschReadingEase >= 50
                                    ? "#eab308"
                                    : "#ef4444",
                              }}
                            >
                              {results.content.readability.fleschReadingEase}
                            </span>
                          </div>
                          <div className="w-full bg-gray-200 rounded-full h-2 mb-2">
                            <div
                              className="h-2 rounded-full transition-all"
                              style={{
                                width: `${Math.min(
                                  results.content.readability.fleschReadingEase,
                                  100
                                )}%`,
                                backgroundColor:
                                  results.content.readability
                                    .fleschReadingEase >= 60
                                    ? "#22c55e"
                                    : results.content.readability
                                        .fleschReadingEase >= 50
                                    ? "#eab308"
                                    : "#ef4444",
                              }}
                            ></div>
                          </div>
                          <div className="flex items-center gap-2 text-xs">
                            <span className="inline-block px-2 py-1 rounded-full bg-blue-100 text-blue-700 font-semibold">
                              {results.content.readability.difficultyRating ||
                                "Standard"}
                            </span>
                            <span className="text-gray-600">
                              {results.content.readability.gradeLevel}
                            </span>
                          </div>
                        </div>

                        {/* Flesch-Kincaid Grade */}
                        {results.content.readability.fleschKincaidGrade && (
                          <div className="mb-4 pb-4 border-b border-gray-200">
                            <div className="flex justify-between items-center">
                              <span className="text-sm font-medium text-gray-700">
                                Grade Level
                              </span>
                              <span className="text-xl font-bold text-indigo-600">
                                {results.content.readability.fleschKincaidGrade}
                              </span>
                            </div>
                          </div>
                        )}

                        {/* Detailed Metrics Grid */}
                        <div className="grid grid-cols-2 gap-3 text-sm mb-4">
                          <div className="bg-gradient-to-br from-blue-50 to-blue-100 p-3 rounded-lg">
                            <div className="text-xs text-gray-600 mb-1">
                              Words
                            </div>
                            <div className="font-bold text-blue-700 text-lg">
                              {results.content.readability.wordCount}
                            </div>
                          </div>
                          <div className="bg-gradient-to-br from-purple-50 to-purple-100 p-3 rounded-lg">
                            <div className="text-xs text-gray-600 mb-1">
                              Sentences
                            </div>
                            <div className="font-bold text-purple-700 text-lg">
                              {results.content.readability.sentenceCount}
                            </div>
                          </div>
                          <div className="bg-gradient-to-br from-green-50 to-green-100 p-3 rounded-lg">
                            <div className="text-xs text-gray-600 mb-1">
                              Syllables
                            </div>
                            <div className="font-bold text-green-700 text-lg">
                              {results.content.readability.syllableCount ||
                                "N/A"}
                            </div>
                          </div>
                          <div className="bg-gradient-to-br from-orange-50 to-orange-100 p-3 rounded-lg">
                            <div className="text-xs text-gray-600 mb-1">
                              Complex Words
                            </div>
                            <div className="font-bold text-orange-700 text-lg">
                              {results.content.readability.complexWordsCount ||
                                0}
                              <span className="text-xs ml-1">
                                (
                                {results.content.readability
                                  .complexWordsPercentage || 0}
                                %)
                              </span>
                            </div>
                          </div>
                        </div>

                        {/* Averages */}
                        <div className="grid grid-cols-2 gap-3 text-sm mb-4">
                          <div className="bg-gray-50 p-3 rounded flex justify-between">
                            <span className="text-gray-600">Avg Sentence</span>
                            <span className="font-semibold">
                              {
                                results.content.readability
                                  .averageSentenceLength
                              }{" "}
                              words
                            </span>
                          </div>
                          <div className="bg-gray-50 p-3 rounded flex justify-between">
                            <span className="text-gray-600">Avg Word</span>
                            <span className="font-semibold">
                              {results.content.readability.averageWordLength ||
                                "N/A"}{" "}
                              chars
                            </span>
                          </div>
                        </div>

                        {/* Reading Time */}
                        {results.content.readability.estimatedReadingTime && (
                          <div className="bg-gradient-to-r from-indigo-50 to-purple-50 p-4 rounded-lg border border-indigo-200 mb-4">
                            <div className="flex items-center gap-2 text-sm">
                              <svg
                                className="w-5 h-5 text-indigo-600"
                                fill="none"
                                stroke="currentColor"
                                viewBox="0 0 24 24"
                              >
                                <path
                                  strokeLinecap="round"
                                  strokeLinejoin="round"
                                  strokeWidth={2}
                                  d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"
                                />
                              </svg>
                              <span className="text-gray-700">
                                Estimated Reading Time:
                              </span>
                              <span className="font-bold text-indigo-700">
                                {results.content.readability
                                  .estimatedReadingTime.total ||
                                  `${results.content.readability.estimatedReadingTime.minutes}m ${results.content.readability.estimatedReadingTime.seconds}s`}
                              </span>
                            </div>
                          </div>
                        )}

                        {/* Warnings */}
                        {results.content.readability.readabilityWarnings &&
                          results.content.readability.readabilityWarnings
                            .length > 0 && (
                            <div className="bg-amber-50 border-l-4 border-amber-500 p-3 rounded text-xs">
                              <div className="font-semibold text-amber-900 mb-1">
                                ⚠️ Suggestions:
                              </div>
                              <ul className="space-y-1 text-amber-800">
                                {results.content.readability.readabilityWarnings.map(
                                  (warning: string, i: number) => (
                                    <li key={i}>• {warning}</li>
                                  )
                                )}
                              </ul>
                            </div>
                          )}
                        <div className="mt-3 flex flex-wrap gap-2 text-xs">
                          {(() => {
                            const badge = (
                              label: string,
                              state: "good" | "warning" | "poor"
                            ) => (
                              <span
                                className={`px-2 py-1 rounded-full ${
                                  state === "good"
                                    ? "bg-green-100 text-green-700"
                                    : state === "warning"
                                    ? "bg-yellow-100 text-yellow-700"
                                    : "bg-red-100 text-red-700"
                                }`}
                              >
                                {label}: {state}
                              </span>
                            );
                            const ind = results.content.indicators;
                            return (
                              <>
                                {badge("Readability", ind.readability)}
                                {badge("Sentence Len", ind.sentenceLength)}
                                {badge("Paragraph Len", ind.paragraphLength)}
                                {badge("Keyword Variety", ind.keywordVariety)}
                              </>
                            );
                          })()}
                        </div>
                      </Section>
                      <Section
                        title="Structure"
                        description="Paragraphs, lists, headings"
                      >
                        <div className="grid grid-cols-2 gap-3 text-sm">
                          <div className="bg-gray-50 p-3 rounded flex justify-between">
                            <span>Paragraphs</span>
                            <span className="font-semibold">
                              {results.content.structure.paragraphCount}
                            </span>
                          </div>
                          <div className="bg-gray-50 p-3 rounded flex justify-between">
                            <span>Avg Para Len</span>
                            <span className="font-semibold">
                              {results.content.structure.averageParagraphLength}
                            </span>
                          </div>
                          <div className="bg-gray-50 p-3 rounded flex justify-between">
                            <span>Lists</span>
                            <span className="font-semibold">
                              {results.content.structure.listCount}
                            </span>
                          </div>
                          <div className="bg-gray-50 p-3 rounded flex justify-between">
                            <span>List Items</span>
                            <span className="font-semibold">
                              {results.content.structure.listItemCount}
                            </span>
                          </div>
                          {Object.entries(
                            results.content.structure.headings
                          ).map(([k, v]: any) => (
                            <div
                              key={k}
                              className="bg-gray-50 p-3 rounded flex justify-between"
                            >
                              <span className="uppercase">{k}</span>
                              <span className="font-semibold">{String(v)}</span>
                            </div>
                          ))}
                        </div>
                      </Section>
                      <Section
                        title="Top Keywords"
                        description="Highest density terms"
                      >
                        <div className="space-y-2 text-sm">
                          {results.content.keywordDensity.topKeywords.map(
                            (k: any) => (
                              <div
                                key={k.keyword}
                                className="flex justify-between bg-gray-50 p-2 rounded"
                              >
                                <span className="font-medium">{k.keyword}</span>
                                <span>
                                  {k.count} ({k.densityPercent}%)
                                </span>
                              </div>
                            )
                          )}
                        </div>
                      </Section>
                      <Section
                        title="Suggestions"
                        description="Improvement opportunities"
                      >
                        <ul className="list-disc list-inside space-y-1 text-sm text-gray-700">
                          {results.content.suggestions.map(
                            (s: string, i: number) => (
                              <li key={i}>{s}</li>
                            )
                          )}
                        </ul>
                      </Section>
                    </>
                  )}
                </div>
              </div>
            )}

            {activeTab === "scores" && results.lighthouse?.scores && (
              <div role="tabpanel" className="animate-fadeIn">
                <h2 className="text-2xl font-bold text-gray-900 mb-6 flex items-center gap-2">
                  <span className="text-3xl">🎯</span>
                  Detailed Scores
                </h2>
                <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
                  <ScoreGauge
                    label="Performance"
                    score={results.lighthouse.scores.performance}
                    color="blue"
                  />
                  <ScoreGauge
                    label="SEO"
                    score={results.lighthouse.scores.seo}
                    color="purple"
                  />
                  <ScoreGauge
                    label="Accessibility"
                    score={results.lighthouse.scores.accessibility}
                    color="green"
                  />
                  <ScoreGauge
                    label="Best Practices"
                    score={results.lighthouse.scores.bestPractices}
                    color="indigo"
                  />
                </div>
              </div>
            )}

            {activeTab === "performance" && (
              <div role="tabpanel" className="animate-fadeIn">
                <h2 className="text-2xl font-bold text-gray-900 mb-6 flex items-center gap-2">
                  <span className="text-3xl">⚡</span>
                  Performance Metrics
                </h2>
                <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
                  <PerformancePanel metrics={results.lighthouse?.metrics} />
                </div>
              </div>
            )}

            {activeTab === "device-comparison" &&
              results.lighthouse?.devices && (
                <div role="tabpanel" className="animate-fadeIn">
                  <h2 className="text-2xl font-bold text-gray-900 mb-6 flex items-center gap-2">
                    <span className="text-3xl">📱</span>
                    Device Comparison Analysis
                  </h2>
                  <DeviceComparison
                    mobile={results.lighthouse?.devices?.mobile ?? null}
                    desktop={results.lighthouse?.devices?.desktop ?? null}
                    comparison={results.lighthouse?.comparison ?? {}}
                    responsive={results.lighthouse?.responsive ?? {}}
                  />
                </div>
              )}

            {activeTab === "security" && (
              <div role="tabpanel" className="animate-fadeIn">
                <h2 className="text-2xl font-bold text-gray-900 mb-6 flex items-center gap-2">
                  <span className="text-3xl">🔒</span>
                  Security Analysis
                </h2>
                <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
                  <SecurityPanel data={results.security} />
                </div>
              </div>
            )}

            {activeTab === "security-headers" && (
              <SecurityHeadersTab url={url} />
            )}

            {activeTab === "seo" &&
              (results.seo?.metadata ||
                results.seo?.headings ||
                results.seo?.images ||
                results.seo?.links) && (
                <div role="tabpanel" className="animate-fadeIn">
                  <h2 className="text-2xl font-bold text-gray-900 mb-6 flex items-center gap-2">
                    <span className="text-3xl">🔍</span>
                    SEO Analysis
                  </h2>
                  <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
                    {results.seo?.metadata && (
                      <Section
                        title="Metadata"
                        description="Title, description, and essential meta tags"
                      >
                        <div className="space-y-2 text-sm">
                          {Object.entries(results.seo.metadata)
                            .filter(([k]) => k !== "issues")
                            .map(([k, v]) => (
                              <div key={k} className="flex justify-between">
                                <span className="text-gray-500 capitalize">
                                  {k.replace(/([A-Z])/g, " $1")}
                                </span>
                                <span
                                  className="font-medium text-gray-800 max-w-[60%] text-right truncate"
                                  title={String(v)}
                                >
                                  {String(v)}
                                </span>
                              </div>
                            ))}
                          {Array.isArray(results.seo.metadata.issues) &&
                            results.seo.metadata.issues.length > 0 && (
                              <div className="pt-2 border-t mt-2">
                                <div className="text-xs text-gray-500 mb-1">
                                  Issues
                                </div>
                                <ul className="list-disc list-inside space-y-1 text-red-600">
                                  {results.seo.metadata.issues.map(
                                    (i: string, idx: number) => (
                                      <li key={idx}>{i}</li>
                                    )
                                  )}
                                </ul>
                              </div>
                            )}
                        </div>
                      </Section>
                    )}
                    {results.seo?.headings && (
                      <Section
                        title="Headings"
                        description="Heading structure by level"
                      >
                        <div className="grid grid-cols-2 gap-3 text-sm">
                          {Object.entries(results.seo.headings)
                            .filter(([k]) => k !== "issues")
                            .map(([k, v]) => (
                              <div
                                key={k}
                                className="bg-gray-50 rounded p-3 flex items-center justify-between"
                              >
                                <span className="text-gray-600 uppercase">
                                  {k}
                                </span>
                                <span className="font-semibold">
                                  {String(v)}
                                </span>
                              </div>
                            ))}
                        </div>
                        {Array.isArray(results.seo.headings.issues) &&
                          results.seo.headings.issues.length > 0 && (
                            <div className="pt-3">
                              <div className="text-xs text-gray-500 mb-1">
                                Issues
                              </div>
                              <ul className="list-disc list-inside space-y-1 text-red-600">
                                {results.seo.headings.issues.map(
                                  (i: string, idx: number) => (
                                    <li key={idx}>{i}</li>
                                  )
                                )}
                              </ul>
                            </div>
                          )}
                      </Section>
                    )}
                    {results.seo?.images && (
                      <Section
                        title="Images"
                        description="Alt text coverage and totals"
                      >
                        <div className="grid grid-cols-2 gap-3 text-sm">
                          {Object.entries(results.seo.images)
                            .filter(([k]) => k !== "issues")
                            .map(([k, v]) => (
                              <div
                                key={k}
                                className="bg-gray-50 rounded p-3 flex items-center justify-between"
                              >
                                <span className="text-gray-600 capitalize">
                                  {k}
                                </span>
                                <span className="font-semibold">
                                  {String(v)}
                                </span>
                              </div>
                            ))}
                        </div>
                        {Array.isArray(results.seo.images.issues) &&
                          results.seo.images.issues.length > 0 && (
                            <div className="pt-3">
                              <div className="text-xs text-gray-500 mb-1">
                                Issues
                              </div>
                              <ul className="list-disc list-inside space-y-1 text-red-600">
                                {results.seo.images.issues.map(
                                  (i: string, idx: number) => (
                                    <li key={idx}>{i}</li>
                                  )
                                )}
                              </ul>
                            </div>
                          )}
                      </Section>
                    )}
                    {results.seo?.links && (
                      <Section
                        title="Links"
                        description="Internal vs external links"
                      >
                        <div className="grid grid-cols-2 gap-3 text-sm">
                          {Object.entries(results.seo.links)
                            .filter(([k]) => k !== "issues")
                            .map(([k, v]) => (
                              <div
                                key={k}
                                className="bg-gray-50 rounded p-3 flex items-center justify-between"
                              >
                                <span className="text-gray-600 capitalize">
                                  {k}
                                </span>
                                <span className="font-semibold">
                                  {String(v)}
                                </span>
                              </div>
                            ))}
                        </div>
                      </Section>
                    )}
                  </div>
                </div>
              )}

            {activeTab === "backlinks" && (
              <div role="tabpanel" className="animate-fadeIn">
                <h2 className="text-2xl font-bold text-gray-900 mb-6 flex items-center gap-2">
                  <span className="text-3xl">🔗</span>
                  Backlink Analysis
                </h2>
                <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
                  <Section
                    title="Backlink Summary"
                    description="High-level backlink metrics"
                  >
                    {!backlinks && (
                      <div className="flex items-center gap-2 text-sm text-gray-500">
                        <svg
                          className="animate-spin h-5 w-5"
                          fill="none"
                          viewBox="0 0 24 24"
                        >
                          <circle
                            className="opacity-25"
                            cx="12"
                            cy="12"
                            r="10"
                            stroke="currentColor"
                            strokeWidth="4"
                          ></circle>
                          <path
                            className="opacity-75"
                            fill="currentColor"
                            d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"
                          ></path>
                        </svg>
                        Loading backlink data...
                      </div>
                    )}
                    {backlinks && (
                      <div className="grid grid-cols-2 gap-3 text-sm">
                        {Object.entries(backlinks).map(([k, v]) => (
                          <div
                            key={k}
                            className="bg-gradient-to-br from-blue-50 to-indigo-50 rounded-lg p-4 border border-blue-100"
                          >
                            <div className="text-gray-600 capitalize text-xs font-medium mb-1">
                              {k.replace(/_/g, " ")}
                            </div>
                            <div className="font-bold text-2xl text-gray-900">
                              {String(v)}
                            </div>
                          </div>
                        ))}
                      </div>
                    )}
                  </Section>
                </div>
              </div>
            )}

            {activeTab === "recommendations" && (
              <div role="tabpanel" className="animate-fadeIn">
                <div className="mb-6">
                  <h2 className="text-2xl font-bold text-gray-900 flex items-center gap-2">
                    <span className="text-3xl">💡</span>
                    Actionable Recommendations
                  </h2>
                  <p className="text-gray-600 mt-2">
                    Priority-based suggestions to improve your website's
                    performance and SEO.
                  </p>
                </div>
                <IssueTable title="" issues={buildIssues()} />
              </div>
            )}

            {activeTab === "raw" && (
              <div role="tabpanel" className="animate-fadeIn">
                <h2 className="text-2xl font-bold text-gray-900 mb-6 flex items-center gap-2">
                  <span className="text-3xl">📄</span>
                  Raw Data Explorer
                </h2>
                <RawDataPanel data={results} />
              </div>
            )}
          </div>
        )}
      </div>
    </div>
  );
};

const SecurityHeadersTab: React.FC<{ url: string }> = ({ url }) => {
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const [report, setReport] = useState<any | null>(null);
  const [expanded, setExpanded] = useState<Record<string, boolean>>({});

  const API_BASE_URL =
    process.env.REACT_APP_API_BASE_URL || "https://bc-worker-env.eba-k8rrjwx.ap-southeast-2.elasticbeanstalk.com/api";

  const checkSecurityHeaders = async () => {
    if (!url) return;

    setLoading(true);
    setError(null);
    setReport(null);

    try {
      // Build API URL - handle if API_BASE_URL already includes /api
      const apiUrl = API_BASE_URL.endsWith("/api")
        ? `${API_BASE_URL}/security-headers?url=${encodeURIComponent(url)}`
        : `${API_BASE_URL}/api/security-headers?url=${encodeURIComponent(url)}`;

      const response = await fetch(apiUrl);
      const data = await response.json();

      if (!response.ok) {
        throw new Error(data.error || "Failed to check security headers");
      }

      if (data.success && data.data) {
        setReport(data.data);
      } else {
        throw new Error("Invalid response format");
      }
    } catch (err) {
      setError(
        err instanceof Error ? err.message : "Failed to check security headers"
      );
    } finally {
      setLoading(false);
    }
  };

  const toggleExpanded = (headerName: string) => {
    setExpanded((prev) => ({ ...prev, [headerName]: !prev[headerName] }));
  };

  const getRiskColor = (riskLevel: string) => {
    switch (riskLevel) {
      case "Critical":
        return "#dc2626";
      case "High":
        return "#f97316";
      case "Medium":
        return "#eab308";
      case "Low":
        return "#84cc16";
      default:
        return "#6b7280";
    }
  };

  const getGradeColor = (grade: string) => {
    switch (grade) {
      case "A+":
        return "#22c55e";
      case "A":
        return "#84cc16";
      case "B":
        return "#eab308";
      case "C":
        return "#f97316";
      case "D":
        return "#ef4444";
      case "F":
        return "#dc2626";
      default:
        return "#6b7280";
    }
  };

  return (
    <div role="tabpanel" className="animate-fadeIn space-y-6">
      <div className="flex items-start justify-between gap-4">
        <div>
          <h2 className="text-2xl font-bold text-gray-900 flex items-center gap-2 mb-2">
            <span className="text-3xl">🛡️</span>
            Security Headers Analysis
          </h2>
          <p className="text-gray-600">
            Analyze HTTP security headers and get actionable recommendations
          </p>
        </div>
        <button
          onClick={checkSecurityHeaders}
          disabled={loading || !url}
          className="px-6 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 disabled:bg-gray-400 disabled:cursor-not-allowed font-medium transition-colors"
        >
          {loading ? "Checking..." : "Check Headers"}
        </button>
      </div>

      {error && (
        <div className="p-4 bg-red-50 border border-red-200 rounded-lg flex items-start gap-3">
          <svg
            className="w-5 h-5 text-red-600 flex-shrink-0 mt-0.5"
            fill="currentColor"
            viewBox="0 0 20 20"
          >
            <path
              fillRule="evenodd"
              d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z"
              clipRule="evenodd"
            />
          </svg>
          <p className="text-red-800">{error}</p>
        </div>
      )}

      {report && (
        <div className="space-y-6">
          {/* Overall Score */}
          <div className="bg-gradient-to-r from-blue-600 to-purple-600 rounded-lg shadow-lg p-6 text-white">
            <div className="grid md:grid-cols-3 gap-6">
              <div className="text-center">
                <div className="text-5xl font-bold mb-2">
                  {report.overallScore}
                </div>
                <div className="text-blue-100">Overall Score</div>
              </div>
              <div className="text-center">
                <div
                  className="text-5xl font-bold mb-2"
                  style={{ color: getGradeColor(report.grade) }}
                >
                  {report.grade}
                </div>
                <div className="text-blue-100">Security Grade</div>
              </div>
              <div className="text-center">
                <div className="text-2xl font-semibold mb-2">
                  {report.summary.present} / {report.headers.length}
                </div>
                <div className="text-blue-100">Headers Present</div>
              </div>
            </div>
          </div>

          {/* Summary Stats */}
          <div className="grid md:grid-cols-4 gap-4">
            <div className="bg-white rounded-lg shadow p-4 border-l-4 border-red-500">
              <div className="text-2xl font-bold text-red-600">
                {report.summary.critical}
              </div>
              <div className="text-sm text-gray-600">Critical Issues</div>
            </div>
            <div className="bg-white rounded-lg shadow p-4 border-l-4 border-orange-500">
              <div className="text-2xl font-bold text-orange-600">
                {report.summary.high}
              </div>
              <div className="text-sm text-gray-600">High Priority</div>
            </div>
            <div className="bg-white rounded-lg shadow p-4 border-l-4 border-yellow-500">
              <div className="text-2xl font-bold text-yellow-600">
                {report.summary.medium}
              </div>
              <div className="text-sm text-gray-600">Medium Priority</div>
            </div>
            <div className="bg-white rounded-lg shadow p-4 border-l-4 border-lime-500">
              <div className="text-2xl font-bold text-lime-600">
                {report.summary.low}
              </div>
              <div className="text-sm text-gray-600">Low Priority</div>
            </div>
          </div>

          {/* Additional Checks */}
          <div className="bg-white rounded-lg shadow-lg p-6">
            <h3 className="text-xl font-bold text-gray-900 mb-4">
              Additional Security Checks
            </h3>
            <div className="grid md:grid-cols-3 gap-4">
              <div className="flex items-center gap-3 p-4 bg-gray-50 rounded-lg">
                <svg
                  className={`w-6 h-6 ${
                    report.additionalChecks.httpsEnforced
                      ? "text-green-600"
                      : "text-red-600"
                  }`}
                  fill="currentColor"
                  viewBox="0 0 20 20"
                >
                  {report.additionalChecks.httpsEnforced ? (
                    <path
                      fillRule="evenodd"
                      d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z"
                      clipRule="evenodd"
                    />
                  ) : (
                    <path
                      fillRule="evenodd"
                      d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z"
                      clipRule="evenodd"
                    />
                  )}
                </svg>
                <div>
                  <div className="font-semibold">HTTPS</div>
                  <div className="text-sm text-gray-600">
                    {report.additionalChecks.httpsEnforced
                      ? "Enabled"
                      : "Not enforced"}
                  </div>
                </div>
              </div>
              <div className="flex items-center gap-3 p-4 bg-gray-50 rounded-lg">
                <svg
                  className={`w-6 h-6 ${
                    report.additionalChecks.sslValid
                      ? "text-green-600"
                      : "text-red-600"
                  }`}
                  fill="currentColor"
                  viewBox="0 0 20 20"
                >
                  {report.additionalChecks.sslValid ? (
                    <path
                      fillRule="evenodd"
                      d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z"
                      clipRule="evenodd"
                    />
                  ) : (
                    <path
                      fillRule="evenodd"
                      d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z"
                      clipRule="evenodd"
                    />
                  )}
                </svg>
                <div>
                  <div className="font-semibold">SSL Certificate</div>
                  <div className="text-sm text-gray-600">
                    {report.additionalChecks.sslDetails || "Valid"}
                  </div>
                </div>
              </div>
              <div className="flex items-center gap-3 p-4 bg-gray-50 rounded-lg">
                <svg
                  className={`w-6 h-6 ${
                    !report.additionalChecks.mixedContent.detected
                      ? "text-green-600"
                      : "text-amber-600"
                  }`}
                  fill="currentColor"
                  viewBox="0 0 20 20"
                >
                  {!report.additionalChecks.mixedContent.detected ? (
                    <path
                      fillRule="evenodd"
                      d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z"
                      clipRule="evenodd"
                    />
                  ) : (
                    <path
                      fillRule="evenodd"
                      d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z"
                      clipRule="evenodd"
                    />
                  )}
                </svg>
                <div>
                  <div className="font-semibold">Mixed Content</div>
                  <div className="text-sm text-gray-600">
                    {report.additionalChecks.mixedContent.detected
                      ? `${report.additionalChecks.mixedContent.count} HTTP resources`
                      : "None detected"}
                  </div>
                </div>
              </div>
            </div>
          </div>

          {/* Headers List */}
          <div className="bg-white rounded-lg shadow-lg p-6">
            <h3 className="text-xl font-bold text-gray-900 mb-4">
              Security Headers
            </h3>
            <div className="space-y-3">
              {report.headers.map((header: any, index: number) => (
                <div key={index} className="border rounded-lg overflow-hidden">
                  <div
                    className="p-4 bg-gray-50 cursor-pointer hover:bg-gray-100 transition-colors"
                    onClick={() => toggleExpanded(header.name)}
                  >
                    <div className="flex items-start justify-between gap-4">
                      <div className="flex items-start gap-3 flex-1">
                        <svg
                          className={`w-6 h-6 ${
                            header.present ? "text-green-600" : "text-red-600"
                          } flex-shrink-0 mt-0.5`}
                          fill="currentColor"
                          viewBox="0 0 20 20"
                        >
                          {header.present ? (
                            <path
                              fillRule="evenodd"
                              d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z"
                              clipRule="evenodd"
                            />
                          ) : (
                            <path
                              fillRule="evenodd"
                              d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z"
                              clipRule="evenodd"
                            />
                          )}
                        </svg>
                        <div className="flex-1 min-w-0">
                          <div className="flex items-center gap-3 mb-1">
                            <h4 className="font-semibold text-gray-900">
                              {header.name}
                            </h4>
                            <span
                              className="px-2 py-0.5 text-xs font-medium rounded-full text-white"
                              style={{
                                backgroundColor: getRiskColor(header.riskLevel),
                              }}
                            >
                              {header.riskLevel}
                            </span>
                          </div>
                          <p className="text-sm text-gray-600">
                            {header.description}
                          </p>
                          {header.present && header.value && (
                            <div className="mt-2 p-2 bg-white rounded border text-xs font-mono text-gray-700 overflow-x-auto">
                              {header.value}
                            </div>
                          )}
                        </div>
                      </div>
                      <div className="text-right flex-shrink-0">
                        <div className="text-xl font-bold text-gray-900">
                          {header.score}/{header.maxScore}
                        </div>
                        <div className="text-xs text-gray-500">points</div>
                      </div>
                    </div>
                  </div>

                  {expanded[header.name] && (
                    <div className="p-4 bg-white border-t space-y-3">
                      {!header.present && (
                        <div className="p-3 bg-red-50 border border-red-200 rounded">
                          <div className="font-medium text-red-900 mb-1">
                            ⚠️ Impact
                          </div>
                          <p className="text-sm text-red-800">
                            {header.impact}
                          </p>
                        </div>
                      )}

                      <div className="p-3 bg-blue-50 border border-blue-200 rounded">
                        <div className="font-medium text-blue-900 mb-1">
                          ✅ Recommended Value
                        </div>
                        <code className="text-sm text-blue-800 break-all">
                          {header.recommendedValue}
                        </code>
                      </div>

                      <div className="p-3 bg-green-50 border border-green-200 rounded">
                        <div className="font-medium text-green-900 mb-1">
                          💡 Implementation
                        </div>
                        <code className="text-sm text-green-800 break-all block font-mono">
                          {header.implementationExample}
                        </code>
                      </div>
                    </div>
                  )}
                </div>
              ))}
            </div>
          </div>

          {/* Recommendations */}
          {report.recommendations && report.recommendations.length > 0 && (
            <div className="bg-white rounded-lg shadow-lg p-6">
              <h3 className="text-xl font-bold text-gray-900 mb-4">
                🎯 Priority Recommendations
              </h3>
              <div className="space-y-3">
                {report.recommendations
                  .slice(0, 5)
                  .map((rec: any, index: number) => (
                    <div
                      key={index}
                      className="p-4 border-l-4 rounded"
                      style={{
                        borderColor: getRiskColor(rec.priority),
                        backgroundColor: `${getRiskColor(rec.priority)}10`,
                      }}
                    >
                      <div className="font-semibold text-gray-900 mb-1">
                        {rec.header}
                      </div>
                      <p className="text-sm text-gray-700 mb-2">{rec.action}</p>
                      <code className="text-xs font-mono bg-white px-2 py-1 rounded border">
                        {rec.implementation}
                      </code>
                    </div>
                  ))}
              </div>
            </div>
          )}
        </div>
      )}

      {!report && !loading && !error && (
        <div className="text-center py-12 text-gray-500">
          <svg
            className="w-16 h-16 mx-auto mb-4 text-gray-400"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              strokeLinecap="round"
              strokeLinejoin="round"
              strokeWidth={2}
              d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"
            />
          </svg>
          <p className="text-lg font-medium mb-2">
            Click "Check Headers" to analyze security headers
          </p>
          <p className="text-sm">
            We'll examine 10+ security headers and provide actionable
            recommendations
          </p>
        </div>
      )}
    </div>
  );
};

export default ScanPage;
